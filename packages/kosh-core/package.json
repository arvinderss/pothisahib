{
  "name": "@pothisahib/kosh-core",
  "version": "0.1.0",
  "private": true,
  "description": "Gurbani Kosh application services: source registry, snapshot ingestion, parsing, structure bootstrap, two-person adoption and publication, accounts, MFA, sessions and audit. Used by the API and the CLI.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./testing": {
      "types": "./src/testing.ts",
      "import": "./src/testing.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "@pothisahib/db": "workspace:*",
    "@pothisahib/domain": "workspace:*",
    "@pothisahib/gurmukhi": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
