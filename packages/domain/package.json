{
  "name": "@pothisahib/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain rules: roles, the server-side permission matrix, state vocabularies and input validation. No I/O.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
