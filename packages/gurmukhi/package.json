{
  "name": "@pothisahib/gurmukhi",
  "version": "0.1.0",
  "description": "Integrity core: Gurmukhi codepoint classification, grapheme segmentation, offset-based tokenisation, diff classification and the single sanctioned comparison-normalisation module. Depends on nothing.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
